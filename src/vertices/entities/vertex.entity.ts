export class Vertex {
  id: number;
  x: number;
  y: number;
  floor: number;

  constructor(id: number, x: number, y: number, floor: number) {
    this.id = id;
    this.x = x;
    this.y = y;
    this.floor = floor;
  }
}
